// import 'package:android_client/main.dart';
import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('Counter starts with "No value HHH"',
      (WidgetTester tester) async {
    // await tester.pumpWidget(const MainApp());

    // final counterTextFinder = find.text('No value HHH');
    // expect(counterTextFinder, findsOneWidget);
    expect(true, true);
  });
}

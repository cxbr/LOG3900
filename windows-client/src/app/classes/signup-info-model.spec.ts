import { SignupInfoModel } from './signup-info-model';

describe('SignupInfoModel', () => {
    it('should create an instance', () => {
        expect(new SignupInfoModel()).toBeTruthy();
    });
});
